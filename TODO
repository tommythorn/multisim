General:
  - drop imm from isa_decode_t
  - classify instructions into alu, jump, branch, load, and store.

oooe:
  - be careful about load/store reordering

runners:
  - step_sscalar_renamed_speculative_oooe()
  - ... and other variants

LM32
  - CSR support/simulation
  - device simulation
  - decode must check for illegal field values

MIPS (FULL) ISA

Symbol table support
  - parse
  - look up code addresses and display as sym[+offset]
